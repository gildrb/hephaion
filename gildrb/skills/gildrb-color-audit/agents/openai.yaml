interface:
  display_name: "Gildrb Color Audit"
  short_description: "Audit semantic color and SVG contrast"
  default_prompt: "Use $gildrb-color-audit to verify semantic colors and every shared theme-adaptive artwork instance across standalone and aggregate routes without changing layout or copy."
