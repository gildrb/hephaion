interface:
  display_name: "Gildrb Publishing"
  short_description: "Release Git-backed Cloudflare Pages safely"
  default_prompt: "Use $gildrb-publishing to push an approved gildrb commit, match the Pages Source, wait for route propagation, verify production, and preserve rollback."
