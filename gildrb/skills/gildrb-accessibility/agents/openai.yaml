interface:
  display_name: "Gildrb Accessibility"
  short_description: "Enforce keyboard and semantic accessibility"
  default_prompt: "Use $gildrb-accessibility to verify semantic headings, DOM/visual order, full-row links, sort names and announcements, focus, keyboard, reduced motion, and assistive output."
