# Homepage

## Entry

- Keep the project title as plain section text.
- Do not add a paragraph summary.
- Do not add a textual case-study call to action.
- Make only the approved logo image link to the case route.
- Keep the anchor accessible and keyboard reachable.
- Preserve the entire image frame.
- Retain responsive source selection.

## Biography

Use exactly:

```text
Designing brands, interfaces, and the systems that connect them.
```

Keep this sentence synchronized across visible and machine-readable surfaces.

## Current Projects

- Filen image links to `/filen`.
- mL7 image links to `/ml7`.
- Do not use `/index/<project>` for canonical navigation.
